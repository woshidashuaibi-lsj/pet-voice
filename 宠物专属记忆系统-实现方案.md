# 宠物专属记忆系统 — 实现方案文档

> 目标：将用户与助手的每次对话转化为宠物专属知识，随时间积累形成「只懂这只宠物」的个性化专家系统。

---

## 一、整体架构

```
┌─────────────────────────────────────────────────────┐
│                    用户对话入口                        │
│         healthchat（健康）/ trainingchat（训练）       │
└──────────────────────┬──────────────────────────────┘
                       │
          ┌────────────▼────────────┐
          │     对话结束触发提取     │
          │  onHide / onUnload      │
          └────────────┬────────────┘
                       │
          ┌────────────▼────────────┐
          │   云函数：extractMemory  │
          │   信息提取 + 置信度评分  │
          └────────────┬────────────┘
                       │
          ┌────────────▼────────────┐
          │    云数据库：pets.memory │
          │   事件流 + 状态快照      │
          └────────────┬────────────┘
                       │
          ┌────────────▼────────────┐
          │    下次对话注入背景       │
          │    分层 System Prompt    │
          └─────────────────────────┘
```

---

## 二、Phase 1 — MVP 基础版（建议首先实现）

### 2.1 目标

能跑通「对话 → 提取 → 存档 → 下次注入」完整链路，让用户感受到「它记住了」。

### 2.2 数据结构

在 `pets` 集合中新增 `memory` 字段：

```javascript
{
  _id: "xxx",
  name: "薯条",
  type: "cat",
  breed: "英短",

  // 新增字段
  memory: {
    health:      [{ content: "2026年3月食欲不振", date: "2026-03", source: "healthchat" }],
    behavior:    [{ content: "喜欢抓沙发左侧角", date: "2026-03", source: "trainingchat" }],
    diet:        [{ content: "不吃鸡肉罐头，喜欢金枪鱼", date: "2026-03", source: "healthchat" }],
    events:      [{ content: "2026-02 做了绝育手术", date: "2026-02", source: "user_input" }],
    personality: ["胆小", "粘人"],
    lastUpdated: "2026-03-23"
  }
}
```

### 2.3 云函数：extractMemory

**触发时机**：用户离开聊天页面时（`onHide` / `onUnload`），将本次完整对话发给大模型提取。

**核心 Prompt**：

```
你是一个宠物信息提取助手。
以下是用户与宠物顾问的完整对话：
[对话内容]

宠物信息：名字={{petName}}，种类={{petType}}，品种={{breed}}

请从对话中提取关于这只宠物的新增关键信息。
规则：
1. 只提取用户明确陈述的事实，不推断
2. 模糊表述（"好像"、"可能"）不提取
3. 已知的通用知识不提取，只提取该宠物的个性化信息

输出 JSON 格式（没有信息的维度输出空数组）：
{
  "health":      ["健康相关具体信息"],
  "behavior":    ["行为特征"],
  "diet":        ["饮食偏好"],
  "events":      ["发生的具体事件，尽量带时间"],
  "personality": ["性格标签"],
  "hasNewInfo":  true/false
}
```

**前端触发代码位置**：`healthchat/index.js` 和 `trainingchat/index.js` 的 `onHide()`

```javascript
onHide() {
  // 只有有对话内容才触发提取
  if (this.data.messages.length > 2) {
    wx.cloud.callFunction({
      name: 'extractMemory',
      data: {
        messages: this.data.messages,
        petName: this.data.petName,
        petType: this.data.petType,
      }
    })
    // 不需要等结果，静默后台执行
  }
}
```

### 2.4 注入背景到对话

在 `askVet` / `askTrainer` 云函数中，先读取宠物档案，将 memory 拼入 system prompt：

```javascript
// 构建 system prompt
function buildSystemPrompt(petName, petType, memory) {
  let prompt = `你是${petName}专属的宠物健康顾问。`

  if (memory) {
    const parts = []
    if (memory.health?.length)      parts.push(`健康历史：${memory.health.map(i=>i.content).join('；')}`)
    if (memory.behavior?.length)    parts.push(`行为特征：${memory.behavior.map(i=>i.content).join('；')}`)
    if (memory.diet?.length)        parts.push(`饮食偏好：${memory.diet.map(i=>i.content).join('；')}`)
    if (memory.personality?.length) parts.push(`性格：${memory.personality.join('、')}`)
    if (memory.events?.length)      parts.push(`重要事件：${memory.events.map(i=>i.content).join('；')}`)

    if (parts.length > 0) {
      prompt += `\n\n关于${petName}你已知的信息：\n${parts.join('\n')}`
      prompt += `\n\n请基于以上背景提供个性化建议，如有关联请主动结合分析。`
    }
  }

  return prompt
}
```

### 2.5 Phase 1 效果

| 对话次数 | 用户体验 |
|---|---|
| 第 1 次 | 通用顾问，只知道名字和品种 |
| 第 3 次 | 记住了饮食偏好和主要行为问题 |
| 第 5 次+ | 能主动关联：「你上次提到它不吃鸡肉……」 |

---

## 三、Phase 2 — 强化版（置信度 + 双轨存储）

### 3.1 新增：置信度评分系统

提取时增加置信度字段，**低于阈值的信息不存档**：

```javascript
// 提取结果新增 confidence 字段
{
  "health": [
    { "content": "两天未进食", "confidence": 95 },      // 明确事实 → 存档
    { "content": "好像有点不舒服", "confidence": 35 }   // 模糊描述 → 不存档
  ]
}
```

**置信度判断规则**（在 Prompt 中明确要求）：

| 信息特征 | 置信度范围 |
|---|---|
| 用户明确陈述具体事实 | 80 - 95 |
| 医生 / 专业机构确诊 | 95 - 100 |
| 用户模糊描述 / 猜测 | 20 - 50 |
| 用户主动否定 | 0（触发删除旧记录）|

**存档阈值**：confidence ≥ 60 才写入数据库。

### 3.2 新增：事件流 + 状态快照双轨制

将 memory 拆分为两个集合：

**集合 1：`pet_memory_snapshot`（状态快照，可覆盖）**

```javascript
// 当前宠物的最新状态，每次更新直接覆盖
{
  petId: "xxx",
  health:      "轻微肾结石（已控制），无其他慢性病",
  diet:        "喜欢金枪鱼冻干，不吃鸡肉，只喝流动水",
  behavior:    "喜欢抓沙发，见陌生人躲猫窝",
  personality: ["胆小", "粘人", "夜间活跃"],
  updatedAt:   "2026-03-23"
}
```

**集合 2：`pet_memory_events`（事件流，只追加）**

```javascript
// 每条独立事件，永久保留，带时间戳
{
  petId:      "xxx",
  content:    "呕吐两次，后自愈",
  category:   "health",
  date:       "2026-03-10",
  confidence: 92,
  source:     "healthchat",
  createdAt:  serverDate
}
```

**查询策略**：
- 日常对话 → 只读 `snapshot`（快，省 Token）
- 用户问「它以前有什么病史」→ 查询 `events`（按需加载）

### 3.3 新增：语义去重

提取新信息时，先把已有 snapshot 传给大模型，让它判断是否重复：

```
已有档案信息：
- 饮食：不吃鸡肉罐头

本次新提取到：
- 「它对鸡肉过敏」

请判断：
1. 与已有信息是否语义重复或矛盾？
2. 如重复：选择更准确的表述覆盖
3. 如矛盾：标记冲突，两条并存，新条目加「最新」标记
4. 如全新：直接新增

输出操作指令：
{ "action": "update/append/conflict", "content": "xxx" }
```

---

## 四、Phase 3 — 高级版（记忆衰减 + 分层注入 + 主动追问）

### 4.1 记忆衰减机制

每条记忆引入有效分数，控制是否注入 prompt：

```javascript
// 有效分数计算
function calcMemoryScore(memory) {
  const daysSince = (Date.now() - new Date(memory.date)) / 86400000

  // 时间衰减（180天后衰减到37%）
  const decay = Math.exp(-daysSince / 180)

  // 引用次数强化（每被引用一次+10%，上限200%）
  const reinforcement = Math.min(1 + memory.mentionCount * 0.1, 2.0)

  // 来源权重
  const sourceWeight = {
    'vet_confirmed': 1.5,   // 医生确诊
    'user_stated':   1.0,   // 用户陈述
    'inferred':      0.6    // 系统推断
  }[memory.source] || 1.0

  return memory.confidence * decay * reinforcement * sourceWeight
}

// 注入时过滤：分数低于阈值的记忆不注入 prompt
const activeMemories = memories.filter(m => calcMemoryScore(m) > 40)
```

### 4.2 分层注入策略

将记忆分三层，控制 Token 消耗：

```
第一层：核心档案（每次必注入，~200 Token）
├── 品种、年龄、绝育状态
├── 已知慢性病
└── 最重要的 3 条饮食/行为特征

第二层：近期动态（最近 30 天，每次注入，~300 Token）
├── 近期健康状况变化
└── 近期行为问题

第三层：历史归档（按需检索，0~500 Token）
├── 超过 30 天的历史事件
└── 仅在用户明确询问历史时查询注入
```

```javascript
// 分层注入实现
async function buildLayeredPrompt(petId, userMessage) {
  // 第一层：永远注入
  const snapshot = await getSnapshot(petId)

  // 第二层：近30天事件
  const recentEvents = await getRecentEvents(petId, 30)

  // 第三层：检测用户是否在问历史
  const isAskingHistory = /以前|历史|之前|过去|曾经/.test(userMessage)
  const historyEvents = isAskingHistory ? await getAllEvents(petId) : []

  return buildPrompt(snapshot, recentEvents, historyEvents)
}
```

### 4.3 主动追问式信息补全

检测档案中的缺失字段，在合适时机主动追问：

```javascript
// 定义档案完整度权重
const FIELD_WEIGHTS = {
  'vaccine_date':    { label: '疫苗接种时间', weight: 9 },
  'spayed':          { label: '是否绝育',     weight: 8 },
  'chronic_disease': { label: '慢性病史',     weight: 8 },
  'allergy':         { label: '过敏史',       weight: 7 },
  'diet_brand':      { label: '常用粮食品牌', weight: 5 },
}

// 每次对话随机概率追问一个缺失字段（避免每次都问，烦人）
function shouldAskForInfo(snapshot) {
  const missing = Object.keys(FIELD_WEIGHTS).filter(k => !snapshot[k])
  if (missing.length === 0) return null
  if (Math.random() > 0.3) return null  // 70% 概率不追问

  // 按权重选最重要的缺失字段
  missing.sort((a, b) => FIELD_WEIGHTS[b].weight - FIELD_WEIGHTS[a].weight)
  return FIELD_WEIGHTS[missing[0]].label
}

// 在回复末尾追加
// "顺便问一下，薯条打过疫苗吗？记录一下方便以后参考～"
```

### 4.4 冲突检测与处理

新旧信息矛盾时，不简单覆盖，而是并存+标记：

```javascript
// 冲突记录结构
{
  field: "behavior",
  old: { content: "不喜欢被抱", date: "2026-01", status: "outdated" },
  new: { content: "变得很粘人，喜欢蜷在主人身上", date: "2026-03", status: "active" },
  conflictNote: "行为可能随年龄/季节发生变化，以最新记录为主"
}

// 注入 prompt 时的处理
// "注意：用户1月反映不喜欢被抱，3月反映变得粘人，
//  可能性格随年龄发生变化，请以最新信息为主，但可提示用户关注此变化。"
```

---

## 五、Phase 4 — 闭环版（用户反馈矫正 + 数据飞轮）

### 5.1 隐式反馈识别

自动识别对话中的矫正信号，无需用户手动操作：

```javascript
// 矫正信号识别 Prompt
`
检查用户的最新回复是否包含对已有档案信息的矫正：
已有信息：「不吃鸡肉」
用户回复：「它现在吃鸡肉了，只是不吃罐头」

如果是矫正，输出：
{ "isCorrection": true, "field": "diet", "oldContent": "不吃鸡肉", "newContent": "不吃鸡肉罐头，干粮可以" }
如果不是，输出：
{ "isCorrection": false }
`
```

### 5.2 档案管理页（用户显式反馈）

在宠物档案页新增「记忆管理」Tab：

```
薯条的专属记忆                    [+ 手动添加]
─────────────────────────────
🏥 健康记录
  • 2026-03 食欲不振（已恢复）    [✓] [✕]
  • 2026-02 绝育手术              [✓] [✕]

🍽️ 饮食偏好
  • 不吃鸡肉罐头                  [✓] [✕] [✏️]
  • 喜欢金枪鱼冻干                [✓] [✕]

🐾 行为特征
  • 喜欢抓沙发                    [✓] [✕]
─────────────────────────────
档案完整度：72%  [去完善]
```

用户点击「✕」删除或「✏️」修改，直接更新数据库，同时记录「用户主动矫正」日志，用于后续优化提取 Prompt。

### 5.3 多模态信息源汇入

除对话外，其他数据源也自动归档：

```
录音分析结果 ──────────→ 情绪趋势记录
  「2026-03-23 情绪=焦虑(87%)」

打卡记录 ──────────────→ 长期健康趋势
  「连续7天情绪评分下滑 → 触发提醒」

用户手动填写 ──────────→ 高可信度档案
  疫苗记录、绝育状态、过敏原
```

**趋势分析触发提醒**：

```javascript
// 打卡数据分析（每周一次）
function analyzeCheckinTrend(checkins) {
  const recent7 = checkins.slice(-7)
  const avgScore = recent7.reduce((s, c) => s + c.score, 0) / 7

  if (avgScore < 60) {
    // 推送提醒
    wx.cloud.callFunction({
      name: 'sendSubscribeMessage',
      data: {
        templateId: 'xxx',
        content: `${petName}最近一周情绪偏低，要不要和健康助手聊聊？`
      }
    })
  }
}
```

---

## 六、各阶段成本对比

| 阶段 | 每次对话额外成本 | Token 增量 | 开发工作量 |
|---|---|---|---|
| Phase 1 MVP | +0.002 元 | +500 Token | 3-5 天 |
| Phase 2 强化版 | +0.003 元 | +600 Token | 5-7 天 |
| Phase 3 高级版 | +0.002 元（分层优化后反而降低）| +400 Token | 7-10 天 |
| Phase 4 闭环版 | +0.001 元 | +300 Token | 10-15 天 |

> 注：Phase 3 通过分层注入，Token 消耗反而比 Phase 2 低。

---

## 七、实施路线图

```
Week 1-2：Phase 1 MVP
  ├── 新增 extractMemory 云函数
  ├── pets 集合增加 memory 字段
  ├── healthchat/trainingchat onHide 触发提取
  └── askVet/askTrainer 注入 memory 背景

Week 3-4：Phase 2 强化版
  ├── 提取结果增加置信度字段
  ├── 拆分 snapshot + events 双集合
  └── 语义去重（大模型判断）

Week 5-6：Phase 3 高级版
  ├── 记忆衰减分数计算
  ├── 分层注入策略
  └── 主动追问补全档案

Week 7+：Phase 4 闭环版
  ├── 隐式反馈识别
  ├── 档案管理页 UI
  └── 多模态数据汇入（录音+打卡）
```

---

## 八、核心价值总结

这套系统本质上是在做三件事：

1. **记忆**：把用户每次输入的碎片信息，结构化存起来
2. **理解**：通过置信度、衰减、去重，让记忆越来越准确
3. **进化**：用户反馈形成闭环，越用越懂这只宠物

最终效果：用户打开小程序，助手能说出——

> 「薯条你好，上次你提到它三月份食欲不振，现在好了吗？最近打卡显示它情绪有点低，结合它胆小的性格，可能是换季导致的……」

**这是从「工具」到「伙伴」的本质跨越，也是产品护城河的来源。**

---

*文档版本：v1.0 | 日期：2026-03-23*

---

## 九、短期对话记忆优化（补充）

### 9.1 问题描述

**现象**：`healthchat` 和 `trainingchat` 页面通过 `messages` 数组维护本轮对话上下文，每次发消息都把历史记录传给云函数（`askVet` / `askTrainer`）。随着对话轮数增加，传入的 Token 数量线性增长，导致：

- **成本激增**：对话进行到第 20 轮时，每次请求携带的历史 Token 可能超过 3000+，是第 1 轮的 10 倍
- **响应变慢**：大模型处理更长上下文时延迟明显上升
- **边际效益低**：10 轮以前的对话对当前回答的参考价值很低

**当前实现**（`healthchat/index.js` 第 158-162 行）：

```javascript
// 当前：取最近 10 条，但随对话增长仍会累积
const historyForCloud = messages
  .slice(-10)
  .filter(m => !m.loading)
  .map(m => ({ role: m.role, content: m.content }))
```

> 注：虽已用 `.slice(-10)` 做了初步截断，但每条消息本身可能很长，10 条仍可能携带大量 Token。

---

### 9.2 解决方案一：基于 Token 长度的滑动窗口截断

**核心思路**：不按条数截断，改为按**字符数（近似 Token 数）**截断，超出阈值时从最旧的消息开始丢弃。

**实现代码**（适用于 `healthchat/index.js` 和 `trainingchat/index.js`）：

```javascript
/**
 * 按字符数截断历史记录，保证传入云函数的上下文不超过阈值
 * @param {Array} messages - 完整消息数组
 * @param {number} maxChars - 最大字符数（约等于 Token 数 × 1.5，默认 2000）
 * @returns {Array} 截断后的消息数组
 */
function buildSlidingWindowHistory(messages, maxChars = 2000) {
  // 过滤掉 loading 占位和空消息
  const valid = messages
    .filter(m => !m.loading && m.content)
    .map(m => ({ role: m.role, content: m.content }))

  let total = 0
  const result = []

  // 从最新的消息往前取，直到超出字符限制
  for (let i = valid.length - 1; i >= 0; i--) {
    const len = valid[i].content.length
    if (total + len > maxChars && result.length > 0) break
    result.unshift(valid[i])
    total += len
  }

  return result
}

// 在 _sendText 中替换原有历史构建逻辑
const historyForCloud = buildSlidingWindowHistory(messages, 2000)
```

**效果**：

| 对话轮数 | 优化前传入字符数 | 优化后传入字符数 |
|---|---|---|
| 第 5 轮 | ~500 | ~500（不变） |
| 第 10 轮 | ~1500 | ~1500（不变） |
| 第 20 轮 | ~4000 | ~2000（截断）|
| 第 50 轮 | ~12000 | ~2000（稳定）|

---

### 9.3 解决方案二：摘要压缩（进阶）

**核心思路**：当历史超过阈值时，不是直接丢弃旧消息，而是调用大模型将旧消息压缩成一段摘要，用摘要替代原始历史，同时保留最近几轮的完整对话。

**流程**：

```
完整历史（20 轮）
       │
       ▼
  超出阈值？ ──否──→ 直接发送
       │是
       ▼
  旧消息（前15轮）──→ 调用大模型压缩
       │                    │
       │              生成摘要（~200字）
       │                    │
       └────────────────────┘
               │
    [摘要消息] + [最近5轮完整对话]
               │
               ▼
         发送给云函数
```

**实现代码**：

```javascript
// 在前端或云函数中执行摘要压缩
async function buildCompressedHistory(messages) {
  const valid = messages.filter(m => !m.loading && m.content)
  const KEEP_RECENT = 6     // 保留最近 6 条完整对话
  const COMPRESS_THRESHOLD = 2000  // 超过 2000 字符才压缩

  const totalChars = valid.reduce((s, m) => s + m.content.length, 0)
  if (totalChars <= COMPRESS_THRESHOLD) {
    // 未超阈值，直接返回
    return valid.map(m => ({ role: m.role, content: m.content }))
  }

  // 超阈值：压缩旧消息
  const oldMessages  = valid.slice(0, -KEEP_RECENT)
  const recentMessages = valid.slice(-KEEP_RECENT)

  // 调用云函数做摘要（或直接在前端调用 API）
  const summaryRes = await wx.cloud.callFunction({
    name: 'summarizeHistory',
    data: { messages: oldMessages }
  })
  const summary = summaryRes.result?.summary || ''

  // 将摘要作为 system 角色消息注入
  return [
    { role: 'system', content: `以下是之前对话的摘要：${summary}` },
    ...recentMessages.map(m => ({ role: m.role, content: m.content }))
  ]
}
```

---

### 9.4 解决方案三：关键信息替代（最优解）

**核心思路**：彻底换一种思路——与其把原始对话历史传给模型，不如把**对话中提炼出的关键信息**传进去。历史越长，提炼后的关键信息反而越精简，Token 消耗趋于稳定甚至下降。

**与 Phase 1 记忆系统的协同**：

这正是 Phase 1 `extractMemory` 云函数的价值所在。每次对话结束后，关键信息已经结构化存入 `pets.memory`，下次对话时直接注入 System Prompt，**不需要再传原始历史**，也不需要摘要，只需几十条关键词即可让模型「记住一切」。

```
❌ 传统方式：
   对话历史（越来越长）→ 模型

✅ 方案三：
   pets.memory（关键信息，始终精简）→ 注入 System Prompt → 模型
   + 当前轮最近 3-4 条对话（保证上下文连贯）
```

**实现代码**：

```javascript
// askVet / askTrainer 云函数中已有的 memory 注入逻辑（Phase 1 已实现）
// 前端只需传入当前轮的少量上下文，无需完整历史

async _sendText(text) {
  const userMsg = { id: this._nextId(), role: 'user', content: text }
  const messages = [...this.data.messages, userMsg]

  // ✅ 方案三：只传最近 3 轮（6条）保持当轮上下文连贯
  // 跨轮的"记忆"由 pets.memory 承担，不依赖历史消息
  const historyForCloud = messages
    .filter(m => !m.loading && m.content)
    .slice(-6)  // 只保留最近 6 条
    .map(m => ({ role: m.role, content: m.content }))

  wx.cloud.callFunction({
    name: 'askVet',
    data: {
      messages: historyForCloud,
      petId:   this.data.petId,    // 云函数通过 petId 读取 memory 注入背景
      petName: this.data.petName,
      petType: this.data.petType,
    }
  })
}
```

**Token 消耗对比**：

```
第 N 次对话（全新一轮）：
  方案一/二：携带上一轮完整历史 → Token 不断累积
  方案三：  只携带 pets.memory 精简背景 + 本轮 6 条 → Token 恒定

pets.memory 注入示意：
  System: "你是薯条专属顾问。
           健康历史：2026-03 食欲不振；2026-02 绝育手术
           饮食偏好：不吃鸡肉，喜欢金枪鱼
           性格：胆小、粘人"
  ≈ 约 100~200 Token，固定不增长
```

**关键优势**：

- **Token 恒定**：无论聊了多少次，每次对话注入的背景都是精简的结构化信息
- **跨会话连贯**：关闭小程序再打开，助手依然「记得」宠物信息（方案一/二做不到）
- **零额外成本**：不需要额外的摘要 API 调用
- **与 Phase 1 天然协同**：extractMemory 提取 → memory 存档 → 下次注入，形成完整闭环

---

### 9.5 三种方案对比

| 维度 | 方案一：滑动窗口 | 方案二：摘要压缩 | 方案三：关键信息替代 |
|---|---|---|---|
| 实现难度 | ⭐ 简单，纯前端 | ⭐⭐⭐ 需要额外云函数 | ⭐⭐ 依赖 Phase 1 已实现 |
| Token 节省 | 中（截断丢弃） | 高（压缩保留） | **最高（结构化精简）** |
| 上下文连贯性 | 当轮连贯，跨轮丢失 | 当轮连贯，跨轮有摘要 | **当轮+跨轮都连贯** |
| 额外 API 成本 | 无 | 每次压缩 ~0.001 元 | **无** |
| 跨会话记忆 | ❌ 不支持 | ❌ 不支持 | **✅ 支持** |
| 适用场景 | 临时补丁方案 | 深度单轮会话 | **长期最优解** |

**推荐策略**：
- **现阶段（Phase 1 已完成）**：直接使用方案三，将 `slice(-10)` 改为 `slice(-6)`，同时确保 `petId` 正确传入云函数
- **方案一**：作为方案三的兜底保险（当 petId 为空、memory 还未建立时降级使用）
- **方案二**：暂不实现，等真正有深度咨询场景需求时再考虑

---

*文档版本：v1.1 | 更新日期：2026-03-24*
